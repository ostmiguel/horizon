-- ── Флаг «используется для оплаты» на счёте ──────────────────────────────────
-- Для фильтра «Счёт откуда» при Расходе: показывать только платёжные инструменты
-- (карты, наличные, резервы, кредитки), скрывать кредиты/займы. По умолчанию —
-- true (всё включено); лишние пользователь выключает вручную. Аддитивно.

ALTER TABLE accounts ADD COLUMN IF NOT EXISTS used_for_payment BOOLEAN NOT NULL DEFAULT true;
